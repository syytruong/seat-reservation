{
  "name": "seat-reservation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "dev:web": "npm run dev -w @seat/web",
    "test": "echo \"No tests configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
